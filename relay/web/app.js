const $ = selector => document.querySelector(selector); let password = sessionStorage.getItem("foorysRelayPassword") || "";
function headers() { return { Authorization: "Basic " + btoa("foorys:" + password), "Content-Type": "application/json" }; }
async function request(url, options = {}) { const response = await fetch(url, { ...options, headers: { ...headers(), ...(options.headers || {}) } }); if (!response.ok) throw new Error((await response.json()).error || "Błąd połączenia"); return response.json(); }
function esc(value) { const e = document.createElement("span"); e.textContent = value; return e.innerHTML; }
async function load() { try { const data = await request("/v1/admin/devices"); $("#devices").innerHTML = data.devices.length ? data.devices.map(device => `<article><b>${esc(device.name)}</b><span class="${device.status === "online" ? "online" : "offline"}">${device.status === "online" ? "online" : "offline"}</span><small>Ostatni kontakt: ${device.lastSeenAt ? new Date(device.lastSeenAt).toLocaleString() : "brak"}</small></article>`).join("") : "<p>Nie ma jeszcze sparowanych dekoderów.</p>"; } catch (error) { $("#notice").textContent = error.message; } }
async function login() { password = $("#password").value; try { await request("/v1/admin/devices"); sessionStorage.setItem("foorysRelayPassword", password); $("#login").hidden = true; $("#dashboard").hidden = false; load(); } catch (_error) { $("#login-error").textContent = "Nieprawidłowe hasło."; } }
$("#sign-in").onclick = login; $("#password").onkeydown = event => { if (event.key === "Enter") login(); }; $("#refresh").onclick = load; $("#sign-out").onclick = () => { sessionStorage.removeItem("foorysRelayPassword"); location.reload(); }; $("#pair").onclick = async () => { try { const data = await request("/v1/admin/pairings", { method: "POST", body: "{}" }); $("#notice").textContent = "Kod parowania ważny 15 min: " + data.pairingCode; } catch (error) { $("#notice").textContent = error.message; } }; if (password) { $("#password").value = password; login(); }
